# BBox GUI

## Requirements

BBox requires Node.js version 20.19+ or 22.12+

## Dependencies

To install dependencies, run the following command:

```bash
npm i
```

## Run

To run the app (default port is 5173), run the following command:

```bash
npm run dev
```
